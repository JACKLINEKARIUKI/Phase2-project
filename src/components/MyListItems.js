import React from "react";

function MyListItems({ items }) {
  return (
    <div>
      {/* {items.map((item) => (
            <MovieCard key={item.id} />
        ))} */}
    </div>
  );
}

export default MyListItems;
